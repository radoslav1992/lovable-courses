name: Build

# Deploys are handled by Cloudflare Workers Builds (connected to this repo),
# which builds and runs `wrangler deploy` on every push to main. This
# workflow is just the PR build check.
on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
